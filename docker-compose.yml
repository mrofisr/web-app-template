services:
  backend:
    build:
      dockerfile: ./Dockerfile.back
    ports:
      - '4000:4000'
  frontend:
    build:
      dockerfile: ./Dockerfile.front
    ports:
      - '8080:8080'
    depends_on:
      - backend
